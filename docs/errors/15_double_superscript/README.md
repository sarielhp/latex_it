# Double superscript

## Description
Two consecutive superscript characters `^` were applied to the same math symbol without grouping braces.

## Remediation
Group the superscripts with curly braces: `$x^{a^b}$` or `$x^{a,b}$`.
