# Missing delimiter (. inserted)

## Description
A `\left` or `\right` macro was not followed by a valid delimiter character.

## Remediation
Provide a delimiter after `\right` (such as `\right)`). Use `\right.` for an empty invisible delimiter.
