# Extra \right

## Description
A `\right` closing delimiter appeared without a corresponding `\left` opening delimiter.

## Remediation
Add `\left` earlier in the formula or replace with `\left.` for a dummy delimiter.
