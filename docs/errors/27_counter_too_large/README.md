# LaTeX Error: Counter too large

## Description
A counter style (like `\fnsymbol`) exceeded its defined pool of symbols (9 max for `fnsymbol`).

## Remediation
Reset footnote counter or use numeric footnote representation.
