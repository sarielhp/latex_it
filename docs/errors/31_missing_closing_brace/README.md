# Missing } inserted

## Description
A math group or macro argument was opened with `{` but never closed before math mode ended.

## Remediation
Add the missing closing brace `}` before the math closing delimiter.
