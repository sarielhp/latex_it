# LaTeX Error: Command \foo undefined

## Description
`\renewcommand` was used on a macro that has not yet been defined.

## Remediation
Use `\newcommand` to define new commands, or verify the macro name spelling.
