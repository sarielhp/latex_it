# Package enumitem Error: key undefined

## Description
An invalid key was supplied to `enumitem` environment options.

## Remediation
Consult enumitem documentation for valid keys (e.g. `label`, `leftmargin`, `topsep`).
